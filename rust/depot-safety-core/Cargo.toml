[package]
name = "depot-safety-core"
description = "Deterministic, no_std protective-field and velocity-arbitration core for the Depot AMR fleet"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
libm.workspace = true

# `libm` is deliberately absent here: a dev-dependency duplicating a dependency is a
# second version waiting to happen, and the tests use the same one the library does.
[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
